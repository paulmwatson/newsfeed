<!DOCTYPE html>
<html lang="<%= I18n.locale %>" class="has-navbar-fixed-bottom<%= ' is-signed-in' if user_signed_in? %>">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
  <meta name="msapplication-TileColor" content="#da532c">
  <meta name="theme-color" content="#f5f5f5">

  <meta name="color-scheme" content="light dark">

  <link rel="manifest" href="/manifest.json">
  <link rel="apple-touch-icon" href="/images/apple-touch-icon.png">
  <link rel="mask-icon" href="/images/safari-pinned-tab.svg" color="#5bbad5">



  <title><%= "#{@menu_text} - " if @menu_text && @menu_text != 'Newsfeed' %>Newsfeed</title>
  <meta name="Description" content="A Really Simple RSS (and ATOM) Newsreader">

  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>

</head>

<body>
  <%= yield %>
  <%= render partial: '/shared/navigation' %>
  <%- if alert || notice -%>
  <div data-controller="toast" data-toast-type="<%= alert ? 'alert' : 'notice' %>"><%= alert || notice %></div>
  <%- end -%>
  <div class="is-position-fixed-top-0 is-border-top-solid-2px-primary" aria-hidden="true"></div>
</body>

</html>